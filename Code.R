# My code in the Homework-0 folder of my first git repository
library(tidyverse)

# Pull:         (cloud) Repo    to  (local) working directory
# Add (stage):  (local) Working to  (local) staging area
# Commit:       (local) Staging to  (local) local repo
# Push:         (local) Local   to  (cloud) Repo

# Click STAGED to add to staging area, 
#   recommented to add .gitignore and .rproj files too
# use .gitignore to prevent files from being synced
